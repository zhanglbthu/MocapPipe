# @package _global_
# Test on 3DPW + 3DPW_OCC
defaults:
  - override /data: mocap/testY
  - override /test_datasets:
      - 3dpw_fliptest
  - override /callbacks:
      - metric/metric_3dpw
  - _self_

task: test

# lightning utilities
pl_trainer:
  devices: 1

logger:
  _target_: pytorch_lightning.loggers.WandbLogger
  project: "gem-test"
  save_dir: "/tmp"
  name: ""
  version: ""
  resume: "allow"
  dir: "/tmp"
  id: null
